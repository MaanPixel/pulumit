name: Pulumi Deployment

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install Pulumi CLI
        run: curl -fsSL https://get.pulumi.com | sh

      - name: Configure Pulumi
        run: pulumi login --cloud-url azurerm://AzureCloud --stack dev

      - name: Pulumi Preview
        run: pulumi preview

      - name: Pulumi Deploy
        run: pulumi up --yes
